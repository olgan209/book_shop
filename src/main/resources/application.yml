#spring.application.name=book_shop
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bs_users
    username: postgres
    password: dataqwe665808
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

